# classify-Drawings-vs.-painting
The code trains a CNN for image classification using the "Art Images" dataset. It preprocesses images, defines two CNN models, trains them, and evaluates their performance. It then predicts the class of an external image.
